{
  "name": "zotero-pdf-trim-addon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "run": "web-ext run --source-dir ./dist",
    "dev": "npm run build && npm run watch & npm run run"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "dompurify": "^3.3.0",
    "html2canvas": "^1.4.1",
    "html2pdf.js": "^0.12.1",
    "jspdf": "^3.0.3"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}
